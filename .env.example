; Copy to .env on every machine/server. The .env copy is gitignored;
; this example is the only version git ever sees.

; Developer admin bypass. Logs in as the first active super_admin.
; Leave either empty to disable the bypass entirely.
DEV_IDENTITY=
DEV_PASSWORD=
